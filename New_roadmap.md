
- [x] The timeline's time marks must be adapted to the zoom level. If I zoom in, I want to be able to see more marks (like every hour not every 6h), this should be a smart grid that adapt to the zoom level.
- [x] When picking a location in the create order form, add the option to create a new location (and use the create location feature with geofence...)
- [x] actors in the actor panel should not be dragable, remove drag icons and drag features. 

- [x] in the orders panel when an order is expanded, it's events should be clickable and go in the sect panel if selected
- [x] clicking an event in the timeline (even a draggable incoming event) should bring the playback to where the click happens.
- [x] selecting an event in the order panel or the actor panel should make the playback jump to the start of that event (and recenter the timeline view on that event and recenter the map on that event)
- [x] Making a load visible, should not create a copy in the timeline of it's associated event. Events are unique in the timeline.
- [x] idle is a valid event with a position. The map should follow it. Right now there is a bug with idle events. Make sure they are working like the other events.



- [x] the playback should not try to force itself into the timeline view when user is scrolling the timeline horizontally, view should not follow playback. There should be two buttons to the left of the zoom options "recenter on Now" " recenter on playback"

- [x] clicking the magnifying glass icons around the zoom slider should increment/decrement the zoom level

- [x] Trucks, trailers, Loads and Drivers are Actors. Actors take part in event. Actors cannot be part of multiple events at the same time. The Header of every actor should always display "is {action verb related to their current event}" nect to their name/number. If they are not currently involved in an event is should show "Is in Waterloo" where Waterloo is the end location of the last event the actor was part of. Additionally the specific action verb is clickable and can change the selected event. For example If I go in the actors panel and expand the trailer section I should see: "#000012 is loading" and if i click on loading it should make the corresponding event as the selected event and highlight it on map and timeline. 

- [x] In the actors panel. Right now There is one title "Actors" and everything is under "actors". I want to have "Actors" and "Geography". keep trucks, drivers, trailers, and loads under actors and locations under geography. Also create a separate Collapsible for hubs (split locations into "Regular Location" and "Hub"). Logically, Hubs and Regular Locations are not actors.

- [x] Let's modify the driver's card. When I expand Actors/drivers I see a driver's card the Header has a truck icon, then says "Truck #0001" the truck icon is enough, I do not need the word "Truck" just the icon and the #0001. Same for trailers, and Loads. Then when I expand the driver, do not show the number again in the list of fields. Also, change "Hub" with "Home Hub" and instead of displaying the uuid, display something similar to "Location" with a toggalable reference. Remove Location, this should not be a stored aspect of the driver, this is legacy code to refactor. In the data model, drivers do not have a location, they have a home hub.

- [x] Remove the "status" field from trailers, locations, drivers, hubs.

- [x] I want to expand on this service to show this is {verb} in/to {location} for every actor's header. By finding the last event. If no event is found, loads must be at their pickup location, while trucks, drivers, and trailers are at their home hub. There is no more "transit", transit should show destination (to {location}). I want the following behaviour: - trucks can be "is bobtailing to {location}" / "is hauling to {location}" / "is deadmiling to {location}" / "is loading in {location}" / "is unloading in {location}", and if the last event they are involved in is in the past, or if they are idling: "is attached in {location}", "is detached in {location}" where the attached/detached verb is determined based on their last attach/detach event. - drivers "is bobtailing to {location}" / "is hauling to {location}" / "is deadmiling to {location}" / "is loading in {location}" / "is unloading in {location}",  and if the last event they are involved in is in the past, or if they are idling: "is on-duty in {location}" or "is off-duty in {location}" for this we need to use hour of services for a driver, for now, to make things simple drivers are on-duty from their day start time for 15 hours, and off-duty the rest of the time. - trailers "is hauling to {location}" / "is deadmiling to {location}" / "is loading in {location}" / "is unloading in {location}", and if the last event they are involved in is in the past, or if they are idling: "is full in {location}" / "is empty in {location}", where the full/empty status is determined based on the last event of type loading or unloading that the trailer was involved in. - loads "is hauling to {location}, "is loading in {location}", "is unloading in {location}" and if the last event they are involved in is in the past, or if they are idling: "is not ready in {location}" / "is ready in {location}" / "is idle in {location}" / "is dropped in {location}". The load is dropped once it has been unloaded at its dropoff destination, it is ready/ not ready if it hasn't been picked up from it's pickup location and the ready/ not ready is determined based on whether the playback is within the pickup window associated to the load's offer or not. is idle means that it is in a trailer idling somewhere.

- [x] Events currently have 3 possible status: incoming, assigned, completed. I want to rename the middle status "assigned" into "dispatched". Currently, there is an "assign" button on incoming orders. This button opens a popup to assign trucks, drivers, and trailer. I want to modify this popup. It will put the playhead 30 min before the loading event for the order (use this time to update the header of actors (verb in location)), it will prompt the user to assign a driver, a truck and a trailer, and it should display the list exactly, like in Actors, except add a radio button in front of the element to select it. Make the popup scrollable if necessary.  If I change things to the header/expanded version of drivers, trucks or trailers, in the future, I want to see the same changes here as well the headers with status must be syncked, and depend on the playhead. When I click to assign a driver, if the driver is on-duty in a hub, auto select (if possible) an empty trailer from that hub, and a detached truck from that hub. If it was not possible to find them, show a warning message to the user. Once, a trailer, truck and driver are found. If they are all in the same spot, create an attach event involving the driver, the trailer, and the truck at the hub location. Then create an event "deadmile" for the 3 of them to go to the pickup location. It also creates a deadmile event for them to go back to the home hub after the load has been unloaded at the dropoff, and detach the truck and trailer in the home hub. These events are incoming events that are added to the associated events of the order. The window is closed upon validation, and the "assign" button on the order header should change and now say "dispatch". The newly associated events must be displayed with the already existing events and the whole order should look like this: attach, dead mile, load, haul, idle, unload, deadmile, detach. 
- [x] There is a bug where I drag the haul, and it still moves after I release my mouse button. (timeline incoming event movement). Delete all the idle events. Do not create them when creating an order. Add logical constraints to the manipulation of haul, unload, load, attach, detach, emptymile. So that none can overlap and each event can only be slide in between its previous and next event (the order must be attach, deadmile to pickup, load, haul to dropoff, unload, deadmile to hub, detach). And also keep the constraints of pickup and dropoff window for load and unload.

- [x] Add a "Road Events" collapsible under "Geography" with a "new" button. When a new road event is created, open a popup with a map and let me click on a road segment (let the user click on the map), zoom and map the click to the closest road segment. (A road segment must be an openstreetmap connected segment that connects two openstreetmap nodes). Let me select multiple road segments by clicking on them, while paning and zooming out. Be careful to not add segments on click and drag. Let me remove road segments from the selection by clicking them again. Once I validate the selection, let me select, max speed, max weight, is usable, event_start (time stamp) and event_end (timestamp), a title, and a note. When I click save validate the entry like in the other forms, let me edit if i entered invalid data, (event ends before it begins). Road events have an internal id, they are permanent objects saved in the db, that appear in the admin panel and can be deleted there. Road events, are events that can be displayed on the timeline (with their own color), and can be displayed on the map as a pin that is located in the baricentre of all its road segments.  Under "Geography/Road Events" when the section is expanded, show the list of existing road events: their title, the note, max speed and max weight, and time info. (make it look similar to other events). Make them clickable and put them in selected event when they are clicked.

- [x] the map must display all events and update everytime the playback moves or everytime an event that intersects with the playback is updated / made visible/ hidden
- [x] clicking on events, in the timeline, in the order panel, on the map should select them. And display them in the select panel

- [x] i cannot click on a road segments while in the adding road events window. I need to be able to click anywhere on map and the clicked coordinate must be maped onto the closest road segment. If road segments are not ready they must be downloaded for the tile that are loaded.

- [x] Map display is broken: only show current events (playback relative). I just created an order and it displays all it s relative events. Even if they are not in the past/future (with respect to playhead).

- [x] Road segment editor is broken. Road segments are not loading. Even when they are not loaded. Here is how it should work: load osm nodes. Load osm paths that connect the nodes. A road segment might be a fraction of a path or a multiple paths, I don't know how it works, but ehat I am concerned about is the nabigable graph of roads. Nodes are intersection, and road segments are the edges of the directed graph that connect those nodes. A one way street only has a one way edge between the two intersection nodes. Everytime we load a tile to view on the map, we should load its nodes and edges (road segments). Load this data. The path is only for visual and for computing distances and projections. The backend data should store nodes and edges. Nodes are just coordinates, and have a geofence-id if they are inside a geofence. Every geofence must have exactly one node, a geofence has also a node-id. A node cannot be in multiple geofences (they cannot overlap). When a geofence is created, make sure there is a node inside. If there are multiple nodes inside, select one randomly. If there is no node inside, check with which road segment the geofence geometry intersect and create a node in the middle of the road segment that is inside the fence, split the road segment into two segments, adjust lengths. If there is no raod segment intersecting make user redraw a fence. Edges have src_node, dst_node, max_weight_kg (weight limit for trucks to use the road), max_speed_km (speed limit), length_km (the length of the segment), and path (the actual geometry of the road segment (it might be curved and not a straight line between the two nodes). 

- [x] when created or updated (draged on the timeline), haul and dispatch events shoudl not simply have a 30min / 2h duration. These events must compute the actual path using the road network and a* with constraints from the road events: max speed and max weight. Hauls and Deadmiles start somewhere and end somewhere. Get the node_id from the location geofence. Then compute the shortest path with a* But you must take road events into account. This is where time comes into play. Let's say the haul starts at 16:00, we get the origin node id, and it has 2 outgoing edges that are candidate for the best path, before choosing any edge (road segment), we must first check for road events who start before 16:00 and end after 16:00, if the road event is associated with the edge that we are looking at, we need to use the constraints from the road event and the edge, we use max speed as the min max speed between the segment and the event, and we use max weight as the minimum as well. Then we check if the road is usable. (load weight + truck weight + trailer weight < max weight), if the segment is not usable, we cannot choose this edge. If it is usable, we compute the expected speed (max speed *0.8) for empty truck / (max speed * 0.7 for heavy loaded truck). From the expected speed, we get the time which is used as the edge weight for A*. 
- [x] Display total time and disstance for deadmile and haul events, in their event card. The position of the pin of the haul and deadmile event must be updated through time (motion) according to the compute itinerary. Th width of the event in the timeline must be fixed to the duration of the haul/deadmile.

- [x] Make sure that when creating/updating a haul event, we use the itinerary and not default time value. When I add a road event if it affects an itinerary. The itinerary must be recomputed. If it leads to a situtation where a delivery cannot be made (road is closed and detour will pass dropoff deadline), flag the order as compromised (deactivate the "dispatch" button if the order is assigned), add a "call client" button that opens an alert with the pickup phone number and the dropoff phone number.

- [x] I cannot see the itinerary changes. I created an order, the haul is a default 2h event. It should not be. It should compute the itinerary from pickup to dropoff, it should show the duration and distance and the event should have appropriate duration in the timeline.


- [x] Road events must have a toggable visibility status. Order relative event must not be shown on map nor timeline if they are toggled off. Road events must not be shown on map if the cursor doesn't intersect with them.

- [x] road events currently have a double pin isue on the map. They have a permanent pin (orange) and when they are selected, there is an additional grey "road Event" delete the grey road event and treat them like events (keep the oragne thing), but let me hide them by cliking the title of the event in the geography panel. and have a blue/with toggle to hide them (hide on map and on timeline). They should behave like normal events in terms of display (react with the playhead position). 

- [x] When the current time reaches an unloading/loading event and there is a truck unloading/loading at the location, send an alert notifying host (use phone number), record the timestamp in the db for dentiton

- [x] moving the playhead must refresh every actor header to show a new status based on the playhead's time. (For example if I play I should see john off duty at 5 am because his day starts at 6am and end 15h later).

- [x] add a split option to orders. When an order is split, it asks with which hub the order should be split. With option to select from list of hubs or create one. A split order becomes two orders as follow: one with the original pickup, dropoff at hub (with no constraint on dropoff window since we own the hub). And a second one with hub as flexible pickup and original drop off with previous constraints. Also, the deadhaul events that are not useful anymore are deleted or modified so that trucks reroute to their home hub when they are done with their haul. 

- [x] add a link order option. When two orders are linked, the same truck, driver, and trailer is reused from the first one to the second one, events are modified in a way so that once the driver has unloaded the first job, it emptymiles to the pickup of the second, load in window, then haul to the 2nd drop, unload there, and finally empty miles back to his home hub. (it is possible to link more orders, but linked orders have events that cannot overlap when manipulated in the timeline. Add a warning option, if the driver's shift extends beyond 14h after the start of his day and enters the off duty territory. 

- [ ] for the slow A* itinerary problem. Do something where the quick heuristic is returned fast, but the complete A* routing is still computed in the background and when it is done computing modify the computed event haul or deadmile and add the correct option. 
 