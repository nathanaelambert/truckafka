## Calculating
- [x] When I click on a load, it takes time to compute the itinerary and doesn't display it right away on the map. It is a bit comfusing for the user. Everytime the map should be updated, but cannot, it should have 50% saturation (looks grey) with a status message as layer on top of the map Saying "updating map" with a simple loading animation (".  ", ".. ", "...", " ..", "  .", "   "). The map should stay interacive during that time, the status message and saturation are cosmetic.
- [x]  Every individual element in "view option" should have a it's own visibility toggle, It should remain visible in the view option list at all time. When an element is selected in display info, it becomes visible, but it doesn't make his all category visible (other trucks do not become visible from selecting one truck). The category should not have a toggle button. The number in parentheis after the category name should also show how many trucks are visible. Instead of "Trucks (23)" it should show "Trucks (1/23)" if only one truck is visible. The category should appear as dispabled (icon and text in grey instead of blue and white) if 0 of its elements are visible. The category collapsible should have two buttons "show all" and "hide all" to toggle every element in the category. Those two buttons are only visible in the category header if the category is expanded.
- [x] When a category is expanded. There is "New" button that allows the user to create a new element of that category. 
- [x] There is extra space underneath the timeline. Use this space to make a staging area. The staging area can hold 0 to 16 elements (trucks, hauls, locations, trailers, loads, drivers, events). Elements share the screen width proportionaly and grow to fill the whole width. Elements can be draged around to be reordered in the staging area. Elements can be removed from the staging area by clicking an "x" icon in the top right corner (close button) of the element card. When an element is added to the staging area, it becomes visible. When an element is removed ("x") from the staging area it becomes invisible. An element from the display info can be draged into the staging area, removing it from the display info stack. every element in the staging area has the same visibility check box as in the view option panel. It corresponds to the same visibility state. To make things clearer, replace the visibility checkbox in both the staging area and the view option with an eye icon that is closed or open based on visibility
- [x] remove the plan page
- [x] remove the road segment toggle and the cluster marker toggle. Clusters should always be active when more than one element are too close to each other. We will take care of road segments later.
- [ ] put the "display info" title and the back and forth arrows and the stack elements info (1/1) on one line  
- [x] Let's rework events and motion: events have an id*(pk), a truck_id(fk), a load_id(fk), a trailer_id(fk), a driver_id(fk), a start_location_id(fk)*, an end_location_id(fk)*, a start_time*, an end_time*, a type*, and a motion_id(fk). motion is a path through space that is timed, and records its distance (the path length). So now, all the actors (loads, trucks, drivers, trailers) can only move by taking part in an event. A start and end location are always required as well as a start and end time. If the start and end location are the same, a motion is not not required (unless the event represents a driver driving in a loop). Here are the event types: -Loading: involves a trailer and a load, can involve a truck(if truck is attached to trailer), can invovle a driver (if driver is in truck), start and end location are the same (no motion), it takes 1 hour. -Unloadind: involves a trailer and a load, can involve a truck (if truck is attached to trailer), can involve a driver (if driver is in truck), start and end location are the same (no motion), it takes 1 hour -Bobtail: involves a truck and a driver, has motion, time depends on path and traffic. -Dead Mile: involves a truck, a trailer and a driver, has motion, time depends on path and traffic. -Haul: involves a truck, a driver, a trailer, and a load, has motion. time depends on path and traffic. -Attach: involves a truck and a trailer, can involve a driver (if driver is in truck), can involve a load (if trailer is loaded) it takes 15min. This changes nothing to the stack and stagin area. Events are the only thing shown in the timeline. Visibility of actors changes, what is displayed on the map changes. The map now displays locations and events pins. Remove pins for loads, trucks, trailers and drivers, Location works as usual. Events are displayed on the map with a pin icon matching their type (loading, unloading, bobtail, deadmile, haul, attach). The pins move to the correct location according to the playhead and their motion. If the playhead is outside the range of existence of the event (before event start or after event end) the event is not displayed anymore. This are the rules for displaying events on map and in the timeline: an event is displayed if and only if at least one of it's actor is visible (while still respecting existence for the map).  

- [x] events tooltip in the timeline look and work like this: - Event type as clickable title (clicking the title centers the camera on the event's location at the time of the playhead) - Actors involved as fields with header like in the display info cards (with two buttons stage, stack, close) (different according to event type) - duration - distance (if has motion)
- [x] the playhead hover showing the time is misplaced. It appears at the very top of the screen (above the map), it should appear in the timeline between the play buttons and the event rows.
- [x] the timeline "Now" button is broken: it stops playback and when playback is resumed the playhead jumps to a fixed point in time in the past. The "now" button should not stop playback, it should only put the playback to the current time. The indicator "past" "future" should indicate "Live" when the playhead is playing at 1x speed at the now time. The now time (live) must be updated live with the current time (so it is always playing at 1x speed).
- [x] Show distances on event tooltip for all event with motion
- [x] When I click on an event's pin on the map. I want to see the same tooptip as on the timeline. In fact, I want the tooltip from the timeline and from the map to be synchronised: there is alway only one event's tooltip visible: it is visible in both the timeline and on the map
- [x] change the zoom buttons on the timeline, make it a slider, for more zoom level options. Make SHIFT + SCROLL be a shortcut to zomm in and out. Show that shortcut when hovering the slider. 
- [x] add an offset to tooltips in the timeline so that they are not hiding the event they correspond to by going over them.
- [x] remove the staging area and it's button. Update all the headers everywhere. They just need one button: close/stack. Rename every "stack" reference and call it "Focus" Rename the "Display Info" as "Focus". 
- [x] Near the "Focus" title add a toggle button to force focus mode. Focus mode overrides visibility elements: it makes all elements in the focus area visible and make all the other invisible (this is a mask that has poriority over the regular visibility options but it does not change visibilities. When focus mode is turned off, visibility comes back to what is was before)
- [x] to the left of the play button add checkboxes to filter events by type.
- [x] remove the status field from trucks and trailers in the cards. 
- [ ] modify timeline zoom option: it should be surrounded by magnifying glass icons with - on the left and + on the rigt. The zoom should use the playhead as center (zoom in around the playhead)
- [ ]  when the player is playing : clicking buttons from an event tooltip from the timeline doesn't work, it only works on the map. Selecting a different event from the map doesn't work when an other event's tooltip is openend. Make the map and timeline share a state of the "selected event" let both and map and timeline update their visuals based on that "selected event". Let the map and timeline both change the "selected event" by clicking on another event (in the timeline) or another event pin (in the map). Let them both set "selected event" to none by closing the tooltip from the currently selected event. Everything works fine when playback is paused, the issue is when playback is playing.
- [ ] merge the timeline control button and the timeline playhead tooltip: use the space where the timeline control buttons are. add the time ticking that is currently in the playhead's tooltip next to the play button.  

